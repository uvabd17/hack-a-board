---
description: Live Display & Ticker
---

Set up the Socket.IO event emitters in the scoring routes.

Build the Projector Display (/h/[slug]/display) with 2-column layout and auto-pagination.

Implement the "Trend" calculation logic (oldRank - newRank).

Git Action: Commit live display system.