---
description: Registration & Dashboard
---

1.Build the marketing homepage (/) with the terminal UI.

2.Build /h/[slug]/register with team creation/joining logic and inviteCode generation.

3.Build the Participant Dashboard (/h/[slug]/dashboard).

4.Git Action: Commit registration flow and dashboard UI.